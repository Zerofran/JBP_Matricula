ahora algo muy importante a tomar en cuenta es el hecho de que tenemos una función de organizar estudiantes, esta puede recurrir a la función que hay en la librería de csv, para organizar las filas de la base de datos, y re instanciar de nuevo los estudiantes para que así aparezcan organizados, como la funcion de organizacion espera que alla encavezados puede que apareca un error si le llega un arrar sin encavezados asi que hay que tener cuidado con eso 


func _on_line_edit_text_changed(new_text: String) -> void:
	var lista = ObjectHUD.get_children()
	for i in lista.size():
		if (str(lista[i].get_name()).to_lower()).similarity(new_text.to_lower()) > 0.3 :
			lista[i].visible = true
		else:
			lista[i].visible = false
	if new_text == "":
		for i in lista.size():
			lista[i].visible = true


Bugs encontrados-------------------------------------------------------------------------
al usar el boton añadir uno nuevo el campo de firmas se daña es como si eliminara las firmas de los demas
pero si edito los que tiene todos pueden representar bien sus firmas.
algo debe haber que falla en la funcion agregar y guardar por que activar y desactivar la edicion de estudiantes
sin tocar las funciones de agregar hace que los estudiantes puedan mostrar sus firmas
